# @package _global_

# to execute this experiment run:
# python train.py experiment=mike-light

defaults:
  - override /data: mike.yaml
  - override /model: light.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["mike", "light", "hfc", "single_speaker", "male", "en_US", "lightspeech"]

run_name: mike-light
