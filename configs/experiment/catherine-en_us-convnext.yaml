# @package _global_

# to execute this experiment run:
# python train.py experiment=catherine-en_us-convnext

defaults:
  - override /data: catherine-en_us.yaml
  - override /model: convnext_tts.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["catherine", "single_speaker", "female", "lessac", "en_US", "convnext"]

run_name: catherine-en_us-convnext
